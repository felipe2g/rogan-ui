import {Meta, Story, Preview, Props} from '@storybook/addon-docs/blocks'
import { withKnobs, text, number, boolean } from "@storybook/addon-knobs";

import { Button } from './Button'

<Meta title="Components/Button" component={Button} />

# Button

This is the `Button`

```jsx
import { Button } from 'my-components'
```

<Preview withSource="open">
  <Story name="Button">
    <Button text="text" />
  </Story>
</Preview>

## Props

## Code playground

<Preview withToolbar>
    <Story name="Button sandbox">
        <Button 
            text={text("text", "submit")}
            borderRadius={number("borderRadius", 10)}
            background={text("background" , "green")}
            fontSize={number("fontSize", 36)}
            padding={text("padding", "10px")}
            data-testid="button"
            />
    </Story>
</Preview>

<Props of={Button} />

## Using different Prop Name
